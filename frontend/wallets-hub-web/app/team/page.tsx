"use client";
import { FormEvent,useState } from "react";import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query";import { Plus,ShieldCheck,Users,X } from "lucide-react";import { Shell } from "@/components/shell";import { api } from "@/lib/api";
type Wallet={id:string;name:string};type Member={id:string;displayName:string;email:string;role:string;isActive:boolean;visibleReceiptDays:number;walletIds:string[];canConfirmReceipts:boolean;canRejectReceipts:boolean;canViewReports:boolean;canExportReports:boolean;canManageDevices:boolean;canManageTeam:boolean};
export default function TeamPage(){const client=useQueryClient();const [open,setOpen]=useState(false);const team=useQuery({queryKey:["team"],queryFn:()=>api<Member[]>("/api/team")});const wallets=useQuery({queryKey:["wallets"],queryFn:()=>api<Wallet[]>("/api/wallets")});const create=useMutation({mutationFn:(body:object)=>api("/api/team",{method:"POST",body:JSON.stringify(body)}),onSuccess:()=>{setOpen(false);client.invalidateQueries({queryKey:["team"]})}});function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const flag=(name:string)=>f.get(name)==="on";create.mutate({displayName:f.get("name"),email:f.get("email"),password:f.get("password"),role:f.get("role"),visibleReceiptDays:Number(f.get("days")),canConfirmReceipts:flag("confirm"),canRejectReceipts:flag("reject"),canViewReports:flag("reports"),canExportReports:flag("export"),canManageDevices:flag("devices"),canManageTeam:flag("team"),walletIds:f.getAll("wallets")})}return <Shell><div className="page-head"><div><span className="eyebrow">People & permissions</span><h1>Team access</h1><p>Control exactly which wallets, history, actions, and reports each person can access.</p></div><button className="btn" onClick={()=>setOpen(true)}><Plus size={18}/>Add team member</button></div><div className="table-wrap"><table><thead><tr><th>Person</th><th>Role</th><th>History</th><th>Wallets</th><th>Receipt access</th><th>Status</th></tr></thead><tbody>{(team.data??[]).map(m=><tr key={m.id}><td><strong>{m.displayName}</strong><br/><span className="muted">{m.email}</span></td><td>{m.role}</td><td>{m.visibleReceiptDays} days</td><td>{m.walletIds.length}</td><td>{m.canConfirmReceipts?"Confirm":"View"}{m.canRejectReceipts?" + reject":""}</td><td><span className={`badge ${m.isActive?"success":"danger"}`}>{m.isActive?"Active":"Disabled"}</span></td></tr>)}</tbody></table></div>{!team.isLoading&&(team.data??[]).length===0&&<div className="empty"><div><Users/><h2>No team members</h2></div></div>}{open&&<div className="modal-backdrop"><form className="modal" onSubmit={submit}><div className="modal-head"><h2>Add team member</h2><button type="button" className="icon-button" onClick={()=>setOpen(false)}><X/></button></div><div className="form-grid"><label>Full name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Temporary password<input name="password" type="password" minLength={8} required/></label><label>Role<select name="role"><option>Employee</option><option>Manager</option><option>Admin</option><option>Owner</option></select></label><label>Visible receipt history (days)<input name="days" type="number" min="1" max="3650" defaultValue="2" required/></label><label>Assigned wallets<select name="wallets" multiple style={{minHeight:110}}>{(wallets.data??[]).map(w=><option value={w.id} key={w.id}>{w.name}</option>)}</select></label></div><p className="eyebrow">Additional permissions</p><div className="permission-grid">{[["confirm","Confirm receipts"],["reject","Reject receipts"],["reports","View reports"],["export","Export reports"],["devices","Manage devices"],["team","Manage team"]].map(([name,label])=><label className="checkbox" key={name}><input type="checkbox" name={name}/>{label}</label>)}</div>{create.error&&<div className="error" style={{marginTop:12}}>{create.error.message}</div>}<div className="button-row" style={{marginTop:18}}><button className="btn" disabled={create.isPending}><ShieldCheck size={17}/>Create access</button><button type="button" className="btn btn-secondary" onClick={()=>setOpen(false)}>Cancel</button></div></form></div>}</Shell>}
